apiVersion: v1
kind: Service
metadata:
  name: ai-finder
  namespace: ai-finder
spec:
  selector: {app: ai-finder}
  type: ClusterIP
  ports:
    - {name: api, port: 80, targetPort: api}
    - {name: metrics, port: 9090, targetPort: metrics}
